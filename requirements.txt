# バージョンは固定する。
# 未固定だと Render がビルドするたびに最新版を解決してしまい、
# 「データコミット由来の再デプロイ」のタイミングで pandas / numpy のメジャー
# 更新を拾って突然起動しなくなる（＝白画面）事故が起こりうる。
# 固定しておけばビルドも速く、Render・GitHub Actions・手元で同じ挙動になる。
pandas==3.0.5
numpy==2.4.4
flask==3.1.3
werkzeug==3.1.8
requests==2.33.1
beautifulsoup4==4.15.0
soupsieve==2.9.1
lxml==6.1.1
gunicorn==26.0.0
